import StyleSheet from 'react-native-media-query';
export const { ids, styles } = StyleSheet.create({
    container: {
        width:'100%',
        height: '100%',
        backgroundColor: '#fff',
    }
});